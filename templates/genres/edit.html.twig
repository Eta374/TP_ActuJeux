{% extends 'base.html.twig' %}

{% block title %}Edit Genres{% endblock %}

{% block body %}
<section class="platedit d-flex justify-content-center align-items-center flex-column">
    <h1>Edit Genres</h1>

    {{ include('genres/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-voirplus mb-1 mt-3 ml-3" href="{{ path('genres_index') }}">back to list</a>

    {{ include('genres/_delete_form.html.twig') }}
</section>

{% endblock %}