{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<section class="useredit d-flex justify-content-center align-items-center flex-column">
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-voirplus mb-1 mt-3 ml-3" href="{{ path('user_index') }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
</section>

{% endblock %}