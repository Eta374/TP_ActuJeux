{% extends 'base.html.twig' %}

{% block title %}Platform{% endblock %}

{% block body %}
<section class="indexnews d-flex justify-content-center align-items-center flex-column">
    <h1>Platform</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform.id }}</td>
            </tr>
            <tr>
                <th>PlatPlatform</th>
                <td>{{ platform.platPlatform }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-voirplus mb-1 ml-3" href="{{ path('platform_index') }}">back to list</a>

    <a class="btn btn-voirplus mb-1 ml-3" href="{{ path('platform_edit', {'id': platform.id}) }}">edit</a>

    {{ include('platform/_delete_form.html.twig') }}
</section>

{% endblock %}