{% extends 'base.html.twig' %}

{% block title %}Edit Platform{% endblock %}

{% block body %}
<section class="platedit d-flex justify-content-center align-items-center flex-column">
    <h1>Edit Platform</h1>

    {{ include('platform/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-voirplus mb-1 mt-3 ml-3" href="{{ path('platform_index') }}">back to list</a>

    {{ include('platform/_delete_form.html.twig') }}
</section>

{% endblock %}