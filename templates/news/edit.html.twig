{% extends 'base.html.twig' %}

{% block title %}Edit News{% endblock %}

{% block body %}
<section class="new d-flex justify-content-center align-items-center flex-column">
    <h1>Edit News</h1>

    {{ include('news/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-voirplus mb-1 mt-3 ml-3" href="{{ path('news_index') }}">back to list</a>
</section>


{% if is_granted('ROLE_ADMIN') %}
{{ include('news/_delete_form.html.twig') }}
{% endif %}
{% endblock %}